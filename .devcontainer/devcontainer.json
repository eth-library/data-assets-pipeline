{
  "name": "DAP Development",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "features": {
    "ghcr.io/devcontainers/features/nix:1": {
      "multiUser": true,
      "extraNixConfig": "experimental-features = nix-command flakes"
    }
  },
  "onCreateCommand": "nix profile add nixpkgs#direnv nixpkgs#nix-direnv && echo 'eval \"$(direnv hook bash)\"' >> ~/.bashrc && echo 'source $HOME/.nix-profile/share/nix-direnv/direnvrc' >> ~/.direnvrc",
  "customizations": {
    "vscode": {
      "extensions": [
        "jnoortheen.nix-ide",
        "ms-python.python",
        "charliermarsh.ruff"
      ],
      "settings": {
        "python.defaultInterpreterPath": "${workspaceFolder}/.venv/bin/python"
      }
    }
  },
  "remoteEnv": {
    "DIRENV_WARN_TIMEOUT": "1m"
  }
}
